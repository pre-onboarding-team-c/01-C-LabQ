const rainfallController = require('./rainfall.controller');
const combinationController = require('./combination.controller');
const drainpipeController = require('./drainpipe.controller');

module.exports = {
  rainfallController,
  combinationController,
  drainpipeController,
};
